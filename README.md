# fridge_consumption

Hackathon submission, won. Disaggregating a household's fridge power draw from its total power readings (NILM-style).

`data/train.csv` is labeled per-home time series with the fridge column; `data/test.csv` is the unlabeled total. `src/model.py` carries the modeling.

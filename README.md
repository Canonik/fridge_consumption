# Fridge Consumption

🏆 **Hackathon-winning project**

This project disaggregates a household's fridge power draw from its total power readings (NILM-style).

`data/train.csv` contains labeled per-home time series with the fridge column; `data/test.csv` contains the unlabeled aggregate readings. The modeling code lives in `src/model.py`.
